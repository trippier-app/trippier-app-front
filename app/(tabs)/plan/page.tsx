import PlaceholderScreen from '@/components/PlaceholderScreen';

export const metadata = { title: 'Plan · Trippier' };

export default function PlanPage() {
  return <PlaceholderScreen tab="plan" />;
}
