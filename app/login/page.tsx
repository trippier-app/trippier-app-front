import LoginScreen from '@/components/LoginScreen';

export const metadata = { title: 'Account · Trippier' };

export default function LoginPage() {
  return (
    <div className="h-dvh overflow-hidden bg-black">
      <LoginScreen />
    </div>
  );
}
