##################################### API ######################################
API_URL=http://localhost:3001

##################################### Maps #####################################
MAPTILER_API_KEY=your_maptiler_api_key
MAPTILER_MAP_ID=your_maptiler_map_id

########################### docker-compose (make) ##############################
FRONT_PORT=3000

# Image coordinates for `make build` / `make push` and the prod pull.
REGISTRY=ghcr.io
OWNER=trippier-app
TAG=latest

################################## production ##################################
FRONT_DOMAIN=app.trippier.dev
EDGE_NETWORK=trippier-public-api_default
IMAGE_TAG=latest
